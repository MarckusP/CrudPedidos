<Window x:Class="CrudPedidos.Views.PessoaFormWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		mc:Ignorable="d"
		Title="Pessoa" Height="220" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>

		<TextBlock Text="ID:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox Grid.Column="1" IsReadOnly="True" Margin="0,0,0,8" Text="{Binding Id}" />

		<TextBlock Grid.Row="1" Text="Nome:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" />

		<TextBlock Grid.Row="2" Text="CPF:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" Text="{Binding Cpf, UpdateSourceTrigger=PropertyChanged}" />

		<StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnSalvar" Content="Salvar" Width="90" Margin="0,10,8,0" Command="{Binding SalvarCommand}" />
			<Button Content="Cancelar" Width="90" Margin="0,10,0,0" IsCancel="True" />
		</StackPanel>
	</Grid>
</Window>


