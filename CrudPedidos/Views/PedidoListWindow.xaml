<Window x:Class="CrudPedidos.Views.PedidoListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:CrudPedidos.Models"
        xmlns:conv="clr-namespace:CrudPedidos.Converters"
        mc:Ignorable="d"
        Title="Pedidos" Height="520" Width="900" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ObjectDataProvider MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}"
                            x:Key="StatusPedidoEnum">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:StatusPedido"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="IsSynchronizedWithCurrentItem" Value="True" />
        </Style>
        <conv:StatusEditavelConverter x:Key="StatusEditavelConverter"/>
    </Window.Resources>

    <DockPanel LastChildFill="True" Margin="8">

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8" HorizontalAlignment="Left">
            <TextBlock Text="ID:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Width="80" Text="{Binding FiltroId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0"/>

            <TextBlock Text="Pessoa:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Width="180" Text="{Binding FiltroPessoa, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0"/>

            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox Width="120"
                      ItemsSource="{Binding Source={StaticResource StatusPedidoEnum}}"
                      SelectedItem="{Binding FiltroStatus, Mode=TwoWay}"
                      Margin="0,0,12,0"/>

            <Button Content="Filtrar" Width="80" Margin="0,0,6,0" Command="{Binding FiltrarCommand}" />
            <Button Content="Limpar" Width="80" Command="{Binding LimparCommand}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8" HorizontalAlignment="Left">
            <Button Content="Incluir" Width="90" Margin="0,0,6,0" Command="{Binding IncluirCommand}" />
            <Button Content="Detalhar" Width="90" Margin="0,0,6,0" Command="{Binding DetalharCommand}" />
            <Button Content="Atualizar o Status" Width="120" Margin="0,0,6,0" Command="{Binding EditarCommand}" />
            <Button Content="Atualizar" Width="90" Margin="0,0,6,0" Command="{Binding AtualizarCommand}" />
            <Button Content="Excluir" Width="90" Margin="0,0,6,0" Command="{Binding ExcluirCommand}" />
        </StackPanel>

        <DataGrid AutoGenerateColumns="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  ItemsSource="{Binding Pedidos}"
                  SelectedItem="{Binding Selected}"
                  CanUserAddRows="False"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                <DataGridTextColumn Header="Data" Binding="{Binding DataVenda, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="160" IsReadOnly="True"/>
                <DataGridTextColumn Header="Pessoa" Binding="{Binding PessoaNome}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Pagamento" Binding="{Binding FormaPagamento}" Width="120" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Status" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding StatusDisponiveis}"
                                      SelectedItem="{Binding Status, Mode=TwoWay}"
                                      IsEnabled="{Binding Status, Converter={StaticResource StatusEditavelConverter}}"
                                      Width="Auto"
                                      HorizontalAlignment="Stretch"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Total" Binding="{Binding ValorTotal, StringFormat={}{0:N2}}" Width="120" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</Window>
