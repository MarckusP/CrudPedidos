<Window x:Class="CrudPedidos.Views.PessoaListWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:CrudPedidos.Views.Converters"
        mc:Ignorable="d"
        Title="Pessoas" Height="450" Width="1152" WindowStartupLocation="CenterOwner">

    <DockPanel LastChildFill="True" Margin="8">
        <DockPanel.Resources>
            <conv:CpfFormatConverter x:Key="CpfFormat" />
        </DockPanel.Resources>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8" HorizontalAlignment="Left">
            <TextBlock Text="Nome:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Width="180" Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0"/>

            <TextBlock Text="CPF:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Width="140" Text="{Binding FiltroCpf, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0"/>

            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <ComboBox Width="117" SelectedValue="{Binding FiltroStatus, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" Margin="0,0,12,0">
                <ComboBoxItem Content="Ativo"/>
                <ComboBoxItem Content="Inativo"/>
                <ComboBoxItem Content="Ambos"/>
            </ComboBox>

            <Button Content="Filtrar" Width="76" Margin="0,0,6,0" Command="{Binding FiltrarCommand}"/>
            <Button Content="Limpar" Width="80" Command="{Binding LimparCommand}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8" HorizontalAlignment="Left">
            <Button Content="Incluir" Width="90" Margin="0,0,6,0" Command="{Binding IncluirCommand}"/>
            <Button Content="Editar" Width="90" Margin="0,0,6,0" Command="{Binding EditarCommand}"/>
            <Button Content="Alterar Status" Width="120" Margin="0,0,6,0" Command="{Binding AlternarAtivoCommand}"/>
            <Button Content="Excluir" Width="76" Margin="0,0,6,0" Command="{Binding ExcluirCommand}"/>
            <Button Content="Pedidos" Width="100" Command="{Binding AbrirPedidosCommand}"/>
        </StackPanel>

        <DataGrid AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  ItemsSource="{Binding Pessoas}" SelectedItem="{Binding SelectedPessoa}"
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="CPF" Binding="{Binding Cpf, Converter={StaticResource CpfFormat}}" Width="150"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusDescricao}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
